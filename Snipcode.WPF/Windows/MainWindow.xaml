<ui:FluentWindow x:Class="Snipcode.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Snipcode.WPF"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Snipcode" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round" d:Background="#202020"
        MinWidth="400" MinHeight="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" FontSize="24" Title="{Binding Title}" />
        <ContentControl Content="{Binding SelectedViewModel}" Grid.Row="1"/>
        <ui:Button Grid.Row="1" Height="50" Width="150" HorizontalAlignment="Center"
                   VerticalAlignment="Center" Content="ChangeView" Command="{Binding ChangeViewCommand}"/>
    </Grid>
</ui:FluentWindow>